dist: xenial
language: python

branches:
  except:
    - gh-pages

python:
  - '3.7'
  #- '3.8'


install:
  - pipenv install --pre --skip-lock

script:
  - python -m crypy.desk.impl.tests
  - python -m crypy.desk.model.tests

notifications:
  email: false

